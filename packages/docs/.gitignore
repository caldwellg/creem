.DS_Store
.idea/
node_modules/
*.lock
.next
.cache
dist
build

# Internal repo dev guide — must never live alongside published docs because
# Mintlify auto-indexes every .md/.mdx under packages/docs/ into
# docs.creem.io/llms-full.txt regardless of docs.json navigation.
CLAUDE.md
